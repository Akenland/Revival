markdown: kramdown
kramdown:
  parse_block_html: true
defaults:
  - scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
      discord_url: "https://discord.akenland.com"
  - scope:
      path: "*.md"
    values:
      layout: "page"
